% aip.sty 25-Mrt-04
\typeout{Document Style option `aip' <8 Mrt 05>.}
% Dit is de stijl voor de examens van AIP
%
% Deze stijl definieert het pagina formaat, graag geen page formatting 
% includes zoals a4wide.sty gebruiken.
%
% Gebruikers macro's
%
% Examen tekst ingesloten in \begin{exam} ... \end{exam}
%
% Vraag
%

% Pagina formaat (gekopieerd van Info 1 examen stijl
    \topmargin 0 pt
    \textheight 24cm
    \oddsidemargin 0.5cm
    \evensidemargin \evensidemargin
    \textwidth 15cm
    \headheight 12pt
    \headsep 12pt
    \raggedbottom

\def\nodateerr{\message{Datum vergeten}\gdef\@datum{}}
\def\notimeerr{\message{Tijd vergeten}\gdef\@tijd{}}
\def\examendatum#1{\def\@datum{#1}}
\def\@datum{\nodateerr}
\def\examentijd#1{\def\@tijd{#1}}
\def\@tijd{\notimeerr}

\newcommand{\streep}{\par\vspace*{0.7\baselineskip}
  \hrule\vspace*{0.7\baselineskip}\par}


\newcounter{cvraag}
\newcounter{cdeelvraag}[cvraag]
\newlength{\somindent}
\settowidth{\somindent}{4.}
\addtolength{\somindent}{0.5em}

\newcommand{\firstpart}{%
  \begin{tabular}{ll}
    Name & : \\[1ex]
    Study program & : \\[1ex]
    ID.\ NR. & :
  \end{tabular}
  \streep
}

\newcommand{\waardering}{%
{\samepage \noindent \vspace{1em} \streep \vspace{1em} \noindent
Points that can be scored per question:
\begin{list}{}{\setlength{\labelwidth}{2cm}
               \setlength{\leftmargin}{2.5cm}
               \setlength{\labelsep}{0.5cm}}
  \listofpoints
\end{list}
\vspace{1em}
Max score that can be obtained: 40 points.\\
The final grade is obtained by dividing the score by 4 and rounding to
the nearest integer. \par
}}

\newenvironment{exam}%
{\gdef\listofpoints{}%
  \firstpart%
  \begin{list}{\textbf{\large \arabic{cvraag}.}}%
    {\usecounter{cvraag}%
      \setlength{\labelsep}{1em}%
      \setlength{\labelwidth}{\somindent}%
      \setlength{\leftmargin}{\somindent}}}%
{\end{list}\waardering \clearpage}

\newenvironment{vraag}[1]%
{\item%
  \toks0=\expandafter{\listofpoints}%
  \xdef\listofpoints{\the\toks0 \noexpand\item[Question \arabic{cvraag}:] #1.}}%
{}

\newenvironment{deelvraag}%
    {\begin{list}{\alph{cdeelvraag}.}%
        {\stepcounter{cdeelvraag}%
         \setlength{\labelsep}{1em}%
         \setlength{\labelwidth}{\somindent}%
         \setlength{\leftmargin}{0pt}%
        }%
     \item
    }%
    {\end{list}}



\def\@oddhead{\underline{\hbox to \textwidth{%
  Exam Adaptive Information Processing (5MB20)
  \hfill\@datum,\quad\@tijd\hfill-\ \number\c@page\ -}}}
\def\@oddfoot{}
\pagenumbering{arabic} \onecolumn \@fileswfalse
